<section class="menu-page">
  <nav>
    <a href="/">Accueil</a>
    {% for item in blog.header_menu %}<a href="{{ item.url }}">{{ item.title }}</a>{% endfor %}
    <a href="/search/">Recherche</a>
  </nav>
</section>
